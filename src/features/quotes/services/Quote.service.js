import axios from "axios";
const API_URL = "https://rmlqmsdev.esoft.co.za/api/";

// axios.interceptors.request.use(function (config) {
//   const user = JSON.parse(localStorage.getItem('user'));
//   config.headers.Authorization = user? `Bearer ${user.token}` :'';
//   return config;
  
//   });
const getproduct = () => {
  return axios.get(API_URL + "products/list");
};
const getproductDetails = (id) => {
    return axios.get(API_URL + `quotes/product-detail/${id}`);
  };





const QuotesInfo = {
    getproduct,
    getproductDetails
};
export default QuotesInfo;



















































































































































































